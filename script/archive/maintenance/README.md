# Archived maintenance scripts

One-off maintenance scripts that were previously in `script/maintenance/`. Kept for reference; run from project root if needed (e.g. `ruby script/archive/maintenance/remove_duplicated_tags.rb`).
