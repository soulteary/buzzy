import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  connect() {
    // Code blocks in Markdown-rendered content can be enhanced with a syntax highlighter here if desired.
  }
}
